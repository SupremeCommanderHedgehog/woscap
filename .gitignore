# Local Claude Code state (machine-specific; do not publish)
.claude/

# Local AI planning/spec scratch work - not part of the published project.
docs/superpowers/

# CI build outputs
/psscriptanalyzer.sarif
/testResults.xml

# DISA STIG content (licensed — never commit)
STIG/

# Pre-publication archive of the private repo this one replaced: the issue/PR
# export plus a git bundle of the ORIGINAL history. The bundle still contains
# docs/superpowers/ and internal lab hostnames that were deliberately purged
# before this repository was made public. NEVER commit or force-add it.
/woscap-full-backup/

# Stray test-fixture zips that Pester's cross-container $script: scope bleed can
# occasionally drop in the repo root (real fixtures build under the system temp dir).
/good.zip
/v1.zip
/v2.zip

# Secret material — never commit. This repo is public and its integrations
# (OpenVAS/GMP, Ansible inventories) are the kind that tempt a local creds file.
.env
.env.*
!.env.example
*.pem
*.key
*.pfx
*.p12
id_rsa
id_ed25519
secrets.*
credentials.*
