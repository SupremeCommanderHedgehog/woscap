# Local Claude Code settings (machine-specific)
.claude/settings.local.json

# Local AI planning/spec scratch work - not part of the published project.
docs/superpowers/

# CI build outputs
/psscriptanalyzer.sarif
/testResults.xml

# DISA STIG content (licensed — never commit)
STIG/

# Stray test-fixture zips that Pester's cross-container $script: scope bleed can
# occasionally drop in the repo root (real fixtures build under the system temp dir).
/good.zip
/v1.zip
/v2.zip
